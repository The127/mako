[package]
name = "shared"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Shared DTOs for the Mako zero-trust KV store"
repository = "https://github.com/The127/mako"

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
