# mako_client

Rust client library for the [Mako](https://github.com/The127/mako) zero-trust KV store.

Unlike the rest of the Mako repository, this crate and the `shared` DTO crate it
depends on are licensed under the [MIT License](LICENSE), so it can be embedded in
any application regardless of that application's license.
